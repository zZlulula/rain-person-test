# 禅意灰绿 UI 实现计划

> **面向 AI 代理的工作者：** 必需子技能：使用 superpowers:subagent-driven-development（推荐）或 superpowers:executing-plans 逐任务实现此计划。步骤使用复选框（`- [ ]`）语法来跟踪进度。

**目标：** 将雨中人心理测试从纯黑底白字改为禅意灰绿浅色主题

**架构：** 逐个文件替换硬编码颜色为 `AppTheme` 常量，按钮/卡片统一走 `GazeChoiceButton` 新样式。逻辑零改动。

**技术栈：** Flutter/Dart，仅改 UI 层（颜色、字重、边框、间距）

---

### 任务 1：AppTheme 配色常量

**文件：** 修改 `lib/app_theme.dart`

- [ ] **步骤 1：添加禅意灰绿色板常量**

```dart
import 'package:flutter/material.dart';
import 'package:google_fonts/google_fonts.dart';

/// 禅意灰绿主题：雨后竹林般的安静治愈感。
class AppTheme {
  AppTheme._();

  // ── 色板 ──
  static const Color bg = Color(0xFFe8ebe4);       // 页面背景
  static const Color surface = Color(0xFFdde2d8);   // 卡片/面板
  static const Color textPrimary = Color(0xFF3a4a3f); // 主文字
  static const Color textSecondary = Color(0x803a4a3f); // 次要文字 ~50%
  static const Color accent = Color(0xFF7a9b8a);    // 高亮/选中
  static const Color border = Color(0x1a3a4a3f);    // 浅边框 ~10%
  static const Color borderStrong = Color(0x403a4a3f); // 深边框 ~25%

  // ── 蒙版（不变）──
  static const Color mask1 = Color.fromARGB(115, 0, 0, 0);
  static const Color mask2 = Colors.black;

  static const Locale locale = Locale('zh', 'CN');

  // ── 主题 ──
  static ThemeData theme() {
    final base = ThemeData(
      useMaterial3: true,
      scaffoldBackgroundColor: bg,
      colorScheme: ColorScheme.fromSeed(
        seedColor: accent,
        brightness: Brightness.light,
      ),
      fontFamily: GoogleFonts.notoSansSc().fontFamily,
      fontFamilyFallback: const [
        'Microsoft YaHei', '微软雅黑', 'PingFang SC',
        'Noto Sans SC', 'SimHei', 'sans-serif',
      ],
    );
    return base.copyWith(
      textTheme: GoogleFonts.notoSansScTextTheme(base.textTheme),
      primaryTextTheme: GoogleFonts.notoSansScTextTheme(base.primaryTextTheme),
    );
  }

  static Widget wrapWithChineseFont(BuildContext context, Widget? child) {
    return DefaultTextStyle(
      style: GoogleFonts.notoSansSc(
        color: textPrimary,
        fontSize: 16,
        height: 1.5,
      ),
      child: child ?? const SizedBox.shrink(),
    );
  }

  static Future<void> preloadFonts() {
    return GoogleFonts.pendingFonts([GoogleFonts.notoSansSc()]);
  }
}
```

- [ ] **步骤 2：验证编译**

```bash
cd "C:/雨中人心理测试" && dart analyze lib/app_theme.dart
```
预期：no errors

- [ ] **步骤 3：Commit**

```bash
git add lib/app_theme.dart
git commit -m "feat: zen grey-green color palette in AppTheme"
```

---

### 任务 2：GazeChoiceButton 新样式

**文件：** 修改 `lib/widgets/gaze_choice_button.dart`

- [ ] **步骤 1：改为线条风格 + 高亮填充**

```dart
import 'dart:math';
import 'package:flutter/material.dart';
import '../app_theme.dart';

/// 视线驱动选项按钮 — 禅意灰绿线条风格
class GazeChoiceButton extends StatelessWidget {
  const GazeChoiceButton({
    super.key,
    required this.label,
    required this.highlighted,
    this.fontSize,
    this.minWidth,
  });

  final String label;
  final bool highlighted;
  final double? fontSize;
  final double? minWidth;

  // ...（screenBase, scaledFontSize, scaledMinWidth, scaledPadding, centeringOffset 保持不变）...

  @override
  Widget build(BuildContext context) {
    final effectiveFontSize = scaledFontSize(context, override: fontSize);
    final effectiveMinWidth = scaledMinWidth(context, override: minWidth);
    final padding = scaledPadding(context);

    return AnimatedContainer(
      duration: const Duration(milliseconds: 200),
      constraints: BoxConstraints(minWidth: effectiveMinWidth),
      padding: padding,
      decoration: BoxDecoration(
        color: highlighted
            ? AppTheme.accent
            : AppTheme.bg.withOpacity(0.3),
        border: Border.all(
          color: highlighted ? AppTheme.accent : AppTheme.borderStrong,
          width: 1,
        ),
        borderRadius: BorderRadius.circular(12),
      ),
      child: Text(
        label,
        textAlign: TextAlign.center,
        style: TextStyle(
          fontSize: effectiveFontSize,
          fontWeight: FontWeight.w300,
          color: highlighted ? Colors.white : AppTheme.textPrimary,
        ),
      ),
    );
  }
}
```

- [ ] **步骤 2：验证编译**

```bash
dart analyze lib/widgets/gaze_choice_button.dart
```

- [ ] **步骤 3：Commit**

```bash
git add lib/widgets/gaze_choice_button.dart
git commit -m "feat: GazeChoiceButton zen line style"
```

---

### 任务 3：Page 1 首页

**文件：** 修改 `lib/pages/page_one.dart`

- [ ] **步骤 1：替换背景 + 字体 + 按钮样式 + 加副标题**

```dart
import 'package:flutter/material.dart';
import '../app_theme.dart';

/// 页面一：首页 — 禅意灰绿
class PageOneView extends StatefulWidget { /* 不变 */ }

class _PageOneViewState extends State<PageOneView> {
  bool _isActionLocked = false;

  @override
  Widget build(BuildContext context) {
    final screenSize = MediaQuery.of(context).size;
    return Scaffold(
      backgroundColor: AppTheme.bg,
      body: Stack(
        children: [
          Positioned(
            left: 0, right: 0, top: screenSize.height * 0.35,
            child: Column(
              mainAxisSize: MainAxisSize.min,
              children: [
                Text(
                  '雨中人',
                  textAlign: TextAlign.center,
                  style: TextStyle(
                    fontSize: 44,
                    fontWeight: FontWeight.w300,
                    letterSpacing: 6,
                    color: AppTheme.textPrimary,
                  ),
                ),
                const SizedBox(height: 12),
                Text(
                  '一场关于内心世界的雨境之旅',
                  textAlign: TextAlign.center,
                  style: TextStyle(
                    fontSize: 14,
                    fontWeight: FontWeight.w300,
                    color: AppTheme.textSecondary,
                  ),
                ),
              ],
            ),
          ),
          Positioned(
            left: 0, right: 0, top: screenSize.height * 0.58,
            child: Center(child: _buildStartButton()),
          ),
        ],
      ),
    );
  }

  Widget _buildStartButton() {
    return GestureDetector(
      onTap: () {
        if (_isActionLocked) return;
        setState(() => _isActionLocked = true);
        widget.onStart();
      },
      child: Container(
        padding: EdgeInsets.symmetric(horizontal: 52, vertical: 16),
        decoration: BoxDecoration(
          border: Border.all(color: AppTheme.borderStrong, width: 1),
          borderRadius: BorderRadius.circular(14),
        ),
        child: Text(
          '开 始',
          style: TextStyle(
            fontSize: 20,
            fontWeight: FontWeight.w300,
            letterSpacing: 4,
            color: AppTheme.textPrimary,
          ),
        ),
      ),
    );
  }
}
```

- [ ] **步骤 2：编译验证**

```bash
dart analyze lib/pages/page_one.dart
```

- [ ] **步骤 3：Commit**

---

### 任务 4：Page 2 准备页

**文件：** 修改 `lib/pages/page_two.dart`

- [ ] **步骤 1：背景 + 文字色 + 按钮色**

改动点：
- `backgroundColor: AppTheme.bg`
- 文案色：`color: AppTheme.textPrimary`
- 标定完成按钮：`color: AppTheme.accent` 背景，白色文字
- 红点保持红色不变

- [ ] **步骤 2：编译验证 + Commit**

---

### 任务 5：Page 3 入场动画

**文件：** 修改 `lib/pages/page_three.dart`

- [ ] **步骤 1：Scaffold 背景 + 文案色 + 按钮色**

改动点：
- `Scaffold backgroundColor: AppTheme.bg`
- 引导文案色：保持白色（叠加在视频+蒙版上）
- GazeChoiceButton 自动继承新样式

- [ ] **步骤 2：编译验证 + Commit**

---

### 任务 6：Page 4 词汇观察

**文件：** 修改 `lib/pages/page_four.dart`

- [ ] **步骤 1：背景 + 分支文案色**

改动点：
- `Scaffold backgroundColor: AppTheme.bg`
- 分支文案色：视频+蒙版上的文字保持白色
- 词汇按钮自动继承 GazeChoiceButton 新样式

- [ ] **步骤 2：编译验证 + Commit**

---

### 任务 7：Page 5 伞/亭子

**文件：** 修改 `lib/pages/page_five.dart`

- [ ] **步骤 1：背景 + 文案色 + 引导文案**

改动点：
- `Scaffold backgroundColor: AppTheme.bg`
- 引导文案色：保持白色（蒙版上）
- 伞/亭子按钮自动继承新样式

- [ ] **步骤 2：编译验证 + Commit**

---

### 任务 8：Page 6 结尾动画

**文件：** 修改 `lib/pages/page_six.dart`

- [ ] **步骤 1：背景 + 文案色**

改动点：
- `Scaffold backgroundColor: AppTheme.bg`
- "天要放晴了"文案：保持白色（蒙版上）
- 左中右按钮自动继承新样式

- [ ] **步骤 2：编译验证 + Commit**

---

### 任务 9：Report 报告页

**文件：** 修改 `lib/pages/report_page.dart`

- [ ] **步骤 1：背景 + 卡片毛玻璃 + 文字色 + 按钮**

```dart
// Scaffold
backgroundColor: AppTheme.bg,

// 标题
style: TextStyle(fontSize: 28, fontWeight: FontWeight.w300,
  letterSpacing: 2, color: AppTheme.textPrimary),

// 总述文案
style: TextStyle(fontSize: 16, color: AppTheme.textSecondary, height: 1.8),

// 卡片
decoration: BoxDecoration(
  color: AppTheme.surface.withOpacity(0.6),
  border: Border.all(color: AppTheme.border),
  borderRadius: BorderRadius.circular(14),
),

// 卡片标题
style: TextStyle(fontSize: 14, color: AppTheme.textSecondary),

// 卡片类型文字
style: TextStyle(fontSize: 20, fontWeight: FontWeight.w400, color: AppTheme.textPrimary),

// 卡片总结
style: TextStyle(fontSize: 15, color: AppTheme.textSecondary, height: 1.6),

// "重新体验"按钮
border: Border.all(color: AppTheme.borderStrong),
borderRadius: BorderRadius.circular(12),
child: Text('重新体验',
  style: TextStyle(fontSize: 18, fontWeight: FontWeight.w300,
    letterSpacing: 2, color: AppTheme.textPrimary)),
```

- [ ] **步骤 2：编译验证 + Commit**

---

### 任务 10：Main.dart — MaterialApp 主题

**文件：** 修改 `lib/main.dart`

- [ ] **步骤 1：确认 `AppTheme.theme()` 已返回 light brightness + bg scaffold**

（任务 1 已做）验证 `MaterialApp` 引用正确。

- [ ] **步骤 2：编译验证**

```bash
cd "C:/雨中人心理测试" && dart analyze lib/
```
预期：no errors

- [ ] **步骤 3：全流程测试**

```bash
flutter run -d windows
```
手动走完整流程，确认所有页面颜色正确、无黑底残留。

- [ ] **步骤 4：Commit + 打 tag**

```bash
git add -A
git commit -m "feat: zen grey-green UI — full theme migration"
git tag -a "禅意灰绿版" -m "禅意灰绿 UI 主题"
```

---

## 验证清单

- [ ] `dart analyze lib/` 零 error
- [ ] 首页：浅灰绿底 + 纤细标题 + 副标题
- [ ] 标定页：浅底深字 + 红点正常
- [ ] 入场动画：蒙版上白字不变
- [ ] 词汇页：六边形按钮线条风
- [ ] 伞/亭子：按钮新样式
- [ ] 结尾动画：方向按钮新样式
- [ ] 报告页：毛玻璃卡片
- [ ] "重新体验"回到首页颜色正确
